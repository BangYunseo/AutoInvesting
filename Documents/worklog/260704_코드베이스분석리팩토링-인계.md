# 260704 코드베이스 분석 & 점진적 리팩토링 — 인계 노트

- 상태: 진행중
- 작성/갱신: 260704

> **이 문서의 목적**: `/clear` 등으로 세션이 초기화돼도, 이 문서 + 작업지시서만 읽으면 동일한 방식으로
> 이어서 작업할 수 있게 하는 인계 노트. 새 세션은 **먼저 아래 "재개 방법"부터** 읽을 것.

---

## 재개 방법 (새 세션이 가장 먼저 할 일)

1. 프로젝트 루트의 **`AUTOINVESTING_ANALYSIS_REFACTOR_PLAN.md`** 를 읽는다 (작업 지시서 = SSOT. git 미추적, 루트에 있음).
2. **`docs/00_overview.md` → `docs/01_architecture.md` → `docs/02_module_priority.md`** 순으로 읽어 전체 그림·진행 순서를 파악한다.
3. `docs/modules/` 의 기존 모듈 노트를 읽는다(이미 끝낸 모듈).
4. 아래 "다음 단계"부터 이어서, "모듈 루프"와 "절대 원칙"을 지키며 진행한다.

---

## 목적 / 요청

바이브 코딩으로 만들어져 오너(사용자)가 전체 구조를 충분히 이해 못 하는 상태의 코드를,
**동작을 바꾸지 않으면서(behavior-preserving)** 이해·정리·문서화한다. 목표는 상품화가 아니라
"오너가 내 도구로 신뢰할 수 있게 이해하기". 지시서의 Phase 0→4를 순서대로, 각 Phase/모듈 종료 시
**요약 보고 + 사용자 확인** 후 다음으로.

## 절대 원칙 (반드시 준수)

1. **동작 보존** — 매매/실행 로직의 의미를 바꾸는 변경은 반드시 먼저 설명·확인. 구조/문서 정리만.
2. **자금/주문·시크릿 코드는 최우선 보수적** — 우선순위 뒤로, 건드릴 땐 별도 확인.
3. **한 번에 하나씩, 작은 커밋** — 모듈 하나 끝날 때마다 커밋 + 요약 보고.
4. **문서화 동시 진행**.
5. **모르면 추측 금지, 질문**.

## 사용자가 내린 결정 (중요 — 그대로 따를 것)

- **레거시/죽은 코드 = 삭제.** 사유: 불필요한 코드가 실제 작업을 오염시킴.
  - 단 경계: **죽은 "코드"는 삭제(git 복구 가능)**, **운영 DB "테이블 물리 DROP"은 영구 손실이라 별도 확인**.
  - `TB_MARKET_SNAPSHOT` 은 규칙상 **보존**(수정·삭제 금지).
- **문서 홈 통합 = 방식 (A)**: 워크플로우 동안 `docs/` 에 쌓고, **Phase 4에서 `docs/` 를 정식 문서 홈으로 확정**,
  낡은 `Documents/` 파일(CODE_MAP·API_REFERENCE 등)은 대체/폐기해 단일 홈으로 통합.
- **모듈 이해 문서 형식 = "오너 관점 요약" + "라인 바이 라인 정독" 두 파트** (남은 모든 모듈에 동일 적용).
  - Part1: 한 문장 역할 / 입력→처리→출력·부작용 / 이 코드가 내리는 결정(평문) / 함정 / 만질 곳 / 불명확 항목.
  - Part2: 핵심 메서드 라인 바이 라인 해설. 끝에 "리팩토링 노트"(무엇을·왜·동작보존 근거).
- **커밋은 진행하되 push는 하지 않음** (push = Render 재배포 트리거 → 항상 명시적 확인 후).
- 설정 파일: `appsettings.local.json` 은 로컬 전용(미필수, Render는 환경변수 사용). `appsettings.json` 은 유지(기본값 시드/문서 역할, 시크릿 칸은 공란).

## 모듈 루프 (Phase 3에서 모듈마다 반복)

이해문서 작성 → (없으면)스모크테스트 → 리팩토링 제안·확인 → 적용 → **빌드+테스트 재검증** → 문서 리팩토링 노트 → 커밋 → 요약 보고 후 다음 모듈.

## 진행 순서 (docs/02_module_priority.md 확정본)

1. ✅ **죽은 코드 제거**  2. ✅ **AppConfigManager + DcaSettings**  3. ⬜ **SimBrokerClient**
4. ⬜ DcaAccumulationEngine  5. ⬜ DailyExecutionService  6. ⬜ 조회 컨트롤러(Price→History→Portfolio→Config→Dca→Auth)
7. ⬜ Utils(Logger→ExchangeRate→Notification→ApiKeyAuth)  8. ⬜ SessionManager→Order/TestController
9. ⬜ **CryptoUtil→KisTokenManager→KisBrokerClient (최고위험, 맨 마지막·매 변경 확인)**

---

## 진행 / 결정 (완료분)

### Phase 0~2 (완료, 커밋 `82d5ddd`)
- `docs/00_overview.md`: 개요·실행법·외부연동·시크릿 위치(이름만)·DB테이블·테스트 유무.
- `docs/01_architecture.md`: 트리거→설정로드→주문→기록 흐름. **핵심 확정 사실**: 적립 모델은 "목표비중"이
  아니라 **"템플릿 고정수량 매수"**(`PlanPurchases`가 비중 계산 안 함). 낡은 `Documents/CODE_READING_GUIDE`·
  `ONBOARDING`(목표비중 서술), `CODE_MAP`·`API_REFERENCE`(삭제된 AI 레이어 서술)는 Phase 4 정정 대상.
- `docs/02_module_priority.md`: 복잡도/위험도/이해필요도 매트릭스 + 위 진행 순서.

### Phase 3 · 1순위 죽은 코드 제거 (완료, 커밋 `85ae43a`)
- 삭제 DTO 6개: `AdaptiveThresholdStatusDto`, `DailyTokenUsageDto`, `StrategySummaryDto`, `AssetMasterDto`, `PriceRangeDto`, `OhlcvDto`.
- 삭제 메서드: `IBrokerClient`/`KisBrokerClient`/`SimBrokerClient` 의 `GetPriceRangeAsync`·`GetOhlcvAsync` + Sim의 미사용 `_rng`.
- 유지: `GetCashBalanceAsync`(PortfolioController 사용).
- 문서: `docs/modules/00_dead-code-removal.md`. 빌드 경고0오류0·테스트 12/12.

### Phase 3 · 2순위 AppConfigManager + DcaSettings (완료, 커밋 `6ce4d68` 정리 / `3cafc37` 문서)
- `AppConfigManager`: 호출처 0인 판단 레이어 키 매핑 삭제(전략/리밸런싱/AI/합의/FX), `SensitiveKeys`에서 `GEMINI_API_KEY` 제거.
- `appsettings.json > Trading`: 미사용 키 제거(`IsPaperTrading`만 유지).
- 문서: `docs/modules/AppConfigManager.md`, `docs/modules/DcaSettings.md`(코드 변경 없음).
- 빌드 경고0오류0·테스트 12/12.

## 아직 안 한 것 / 보류 (다음 단계에서 처리)

- **죽은 테이블 스키마 정리 보류**: `TB_SELL_PLAN`, `TB_TOKEN_USAGE`, `TB_AI_PERFORMANCE`, `TB_INVEST_STRATEGY`,
  `TB_ASSET_MASTER` (코드 참조 0). `create_tables.sql` + `DBManager.InitializeDatabase()` 마이그레이션과 얽혀 있어,
  **Data 레이어(DBManager) 다룰 때** 운영 DB 물리 DROP 여부와 함께 별도 확인 후 진행. (스키마 CREATE문 제거는
  기존 DB엔 영향 없음/신규 DB만 미생성 → 그래도 사용자 확인 후.)
- **낡은 `Documents/` 문서 정정/통합**: Phase 4에서 `docs/` 로 통합하며 처리.

## 다음 단계 (여기서 이어가기)

- **3순위: `SimBrokerClient`** 이해 문서(`docs/modules/SimBrokerClient.md`, 위 2파트 형식) 작성.
  - 시뮬레이션 브로커(가상 시세 ±/기준가, 가상 잔고, 주문 시 잔고 반영). KIS 키 없이 전체 적립 사이클 검증용.
  - 코드 변경은 없을 가능성 높음. 있으면 제안→확인 후.
- 이후 4순위 `DcaAccumulationEngine`(적립 심장부): 이해필요도 최고이나 실주문 위험 → **반드시 `IS_PAPER_TRADING`(Sim)
  + `PlanPurchases` 단위테스트로만** 검증, 로직 의미 불변.

## 검증 방법 (동작 보존 확인 표준)

- `dotnet build AutoInvest.csproj` (경고0·오류0 기대) + `dotnet test Tests/AutoInvest.Tests.csproj` (**12/12 통과** 기대).
- 신규/변경 로직은 `IS_PAPER_TRADING`(SimBroker) 모드 우선 검증. 배분 로직은 `PlanPurchases`/`SelectTemplate` 순수함수 단위검증.

## 커밋 시 주의 (실수 방지)

- **`git add -A` 쓰지 말 것** — 루트의 작업지시서(`AUTOINVESTING_ANALYSIS_REFACTOR_PLAN.md`, 의도적 untracked)가
  딸려 들어감. 변경 경로를 **명시적으로** add 한다. (1순위 커밋 때 실수 → amend로 제거한 이력 있음.)
- 커밋 메시지: 한국어, `<type>: <제목>`(scope 금지), 성격별 분리(remove/docs 등).
- push는 사용자 명시 확인 후에만.

## 관련 커밋 · 파일 (값 아닌 이름만)

- 커밋: `82d5ddd`(Phase0-2 문서) → `85ae43a`(죽은코드제거) → `6ce4d68`(설정정리) → `3cafc37`(설정문서). **모두 미push.**
- 산출물: `docs/00_overview.md`, `docs/01_architecture.md`, `docs/02_module_priority.md`,
  `docs/modules/00_dead-code-removal.md`, `docs/modules/AppConfigManager.md`, `docs/modules/DcaSettings.md`.
- 지시서: `AUTOINVESTING_ANALYSIS_REFACTOR_PLAN.md`(루트, untracked).
